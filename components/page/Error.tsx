import React from 'react';

type ErrorProps = {
    code: number;
    title: string;
}

const Error = (props: ErrorProps) => {
    return (
        <div>

        </div>
    );
};

export default Error;